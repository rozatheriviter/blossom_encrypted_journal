# Blossom — To-Do

- [x] Passphrase change — change a vault's passphrase without deleting it ✓
- [x] Export journal — export as plain text/markdown (decrypted) or as an encrypted backup (.vault file) ✓
- [x] Import journal — import a plaintext/markdown export back into a vault, or restore an encrypted .vault backup ✓
- [x] Word count — live word / character counter in the editor ✓
- [x] Distraction-free mode — collapse sidebar and bottom bar, leaving just the text ✓
- [x] Auto-lock — lock the vault after N minutes of inactivity ✓
- [x] Keyboard shortcuts — Ctrl+N new entry, Ctrl+F search, Ctrl+, font settings ✓
- [x] Export entry — save a single entry as .txt or .md ✓
- [x] Pinned entries — star/pin an entry to keep it at the top of the list ✓
- [x] bottom bar should go vertical when resizing width too narrow so that window can be snapped. important on 720p screen. ✓
- [x] attached media should be able to be viewed in app, or at least enlarged. ✓
- [x] program should not remember last file path added. this is a vulnerability ✓
